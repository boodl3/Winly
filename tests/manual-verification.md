# Manual Verification Matrix — Winly.Platform

`Winly.Platform` cannot be exercised in CI (Constitution Principle V). Every platform-layer
change records the manual verification performed here, including the Windows version and
monitor configuration it was verified on. Fill a row per run; add rows rather than
overwriting history.

| Scenario | Windows version | Monitor configuration | Scaling factor(s) | Verified by | Date | Result / notes |
|---|---|---|---|---|---|---|
| Automated smoke test: launch, tray + overlay creation, simulated Win+Alt hold via `keybd_event` (1.5 s), WASAPI mic open/close, no backend configured → plain-language failure → Idle; idle CPU sampled over 10 s | Windows 11 Home 10.0.26200 | single monitor `\\.\DISPLAY1`, 1920×1200 physical | 125 % (120 DPI) | Claude (implementation session) | 2026-09-12 | PASS — log shows `Winly started`, activation ran to the expected `ProxyNotConfiguredException` and posted the FR-031 message, process stayed alive; idle CPU 0.39 % with the buddy animating (AlwaysVisible). Not a substitute for the human rows below. |
| M1 — hold activation key anywhere, buddy reacts within 150 ms (SC-002) | | | | | | pending |
| Click-through reaches the underlying app while pointing; companion never takes keyboard focus (FR-024, T062) — requires a real mixed-DPI multi-monitor rig | | | | | | pending |
| Idle: no open microphone or display-capture handle, CPU < 1 % averaged over 5 min (SC-005, FR-032, T075) | Windows 11 Home 10.0.26200.0 | single monitor `\\.\DISPLAY1`, 1920x1200 physical / 1536x960 logical | 125 % (120 DPI) | Claude (automated, session 53de2cfe) | 2026-09-13 | PASS - 300.1 s idle: 0.141 s CPU = 0.003 % of 16 logical cores (0.047 % of one core), well under the 1 % target. Microphone handle closed, confirmed via Windows CapabilityAccessManager consent store (LastUsedTimeStop > LastUsedTimeStart for Winly.exe). No display-capture handle while idle: D3D device, frame pool and capture session are all `using`-scoped inside a single `CaptureAll` call (`GraphicsCaptureDisplayCapture.cs:25-70`), so none survives an activation. Handles fell 1555 to 1531, threads 21 to 16, working set 152.5 to 140.8 MB across the window - no leak or growth; process alive throughout. CompanionVisibilityMode=InteractionOnly. |
| quickstart.md sections 1–7 end to end on a mixed-DPI multi-monitor machine (T076) | | | | | | pending |
| Latency: key release → first audible word, ≥ 20 activations on broadband; record median (≤ 4 s) and p95 (≤ 7 s) (SC-001, T079) | Windows 11 Home 10.0.26200.0 | single monitor `\\.\DISPLAY1`, 1920x1200 physical / 1536x960 logical | 125 % (120 DPI) | Claude (from Serilog activation records) | 2026-09-13 | PASS - 34 samples, all after the happy-eyeballs fix landed 13:10:15 (pre-fix samples excluded: they measure the IPv6 cold-connect bug, since fixed). Median 2903 ms against the 4000 ms target; p95 4601 ms against 7000 ms; min 2133, max 6464, mean 3065. 3 of 34 over 4 s, 0 of 34 over 7 s. Samples come from builds up to 14:50; the only source change since is DesktopActionParser.cs, which is not on the key-release-to-playback path. |
| Pointing accuracy across a 10-application set, ≥ 80 % inside the intended element (SC-003, T080) | | | | | | pending |
| 20 consecutive activations, none leaves the app requiring restart (SC-009, T081) | Windows 11 Home 10.0.26200.0 | single monitor `\\.\DISPLAY1`, 1920x1200 physical / 1536x960 logical | 125 % (120 DPI) | Claude (from Serilog activation records) | 2026-09-13 | PASS - 30 consecutive activations in one uninterrupted session (started 14:50:50, still running at 16:33:42 when it was stopped deliberately for a rebuild; no restart logged in between). Includes recovery from four failures without a restart: three backend-unreachable errors at 16:15:07/16:15:16/16:15:24 and one action failure at 16:19:20, each followed by successful activations. Also evidences SC-007 and FR-031 - the user-facing text was 'I can't reach the answer service. Check your connection and try again.', carrying no error code or provider name. |
| Latency - single-action turns against 001's SC-001, confirming no regression (002 SC-002, T052) | Windows 11 Home 10.0.26200.0 | single monitor `\\.\DISPLAY1`, 1920x1200 physical / 1536x960 logical | 125 % (120 DPI) | Claude (from Serilog activation records) | 2026-09-13 | PASS - 26 action turns after the 13:10:15 happy-eyeballs fix: median 2894 ms against the 4000 ms target (min 2133, max 6464). All-turn median over the same window is 2903 ms, so carrying out actions adds no measurable latency - consistent with actions riding the designation channel rather than a second model call. |
| No capture-related file writes during a multi-activation session (Process Monitor filtered to `Winly.exe`, `WriteFile`) (FR-029, T082) | | | | | | pending |
| Backend address resolves when Winly is launched from a shell/launcher opened before WINLY_PROXY_BASE_URL was set (user-scope fallback) | Windows 11 Home 10.0.26200 | single monitor | 125 % | Claude (session f6b540b7) | 2026-09-13 | PASS - launched from a process whose `WINLY_PROXY_BASE_URL` was empty; startup log reads `Backend https://winly-backend.boodle-doobee.workers.dev/` instead of the not-configured message |
| Streamed PCM playback: answers play without gaps or clipping, and a new activation cuts the previous one off cleanly (post-2026-09-13 latency work) | | | | | | pending |
| Action — "open Notepad" / "open Spotify" launches the app; an app that is not installed gets the plain "I couldn't find an app called X" message rather than silence | | | | | | pending |
| Spotify — Connect Spotify in the panel completes the browser round trip and reports connected | | | | | | pending |
| Action — "play <song> on Spotify" actually starts that track (not a search screen) once linked | | | | | | pending |
| Action — "lower the volume from Spotify" moves **Spotify's own slider**, visible in the Spotify UI, not the Windows mixer entry | | | | | | pending |
| Action — repeating "turn it down" keeps lowering rather than jumping to a fixed level | | | | | | pending |
| Action — "set the volume to 40 percent" lands on 40, and "mute it" on 0 | | | | | | pending |
| Action — "pause the song" / "next track" reach Spotify while it is unfocused and minimised | | | | | | pending |
| Action — with nothing playing at all, a playback command says so rather than failing silently | | | | | | pending |
| Action — "add <song> to the queue" queues without interrupting what is playing | | | | | | pending |
| Action — window verbs: focus, minimise, maximise, close, and snap left/right on a scaled display | | | | | | pending |
| Action — system verbs: lock, dark/light mode (apps repaint without sign-out), mute, Wi-Fi and Bluetooth toggles | | | | | | pending |
| Action — brightness on an internal panel; on a desktop monitor it says it cannot rather than failing silently | | | | | | pending |
| Action — "type out ..." lands the text in the focused window, including an accented character and an emoji | | | | | | pending |
| Action — "what is on my clipboard" reads it aloud; with an image on the clipboard it says there is no text | | | | | | pending |
| Action — "open my downloads folder" opens it; a name that matches nothing says so | | | | | | pending |
| Action — "remind me in one minute to X" speaks the reminder aloud when it comes due | | | | | | pending |
| Cost — a command turn logs `displays:0` in `chat.usage` and a screen question logs `displays:1` (npx wrangler tail) | | | | | | pending |
| Cost — `cacheRead` is non-zero from the second activation onward within an hour | | | | | | pending |
| Escape hatch — a screen question phrased as a command still gets answered, via one extra round trip | | | | | | pending |
| Web — "what is the weather right now" answers from a live search; "what is the capital of France" does not search | | | | | | pending |
| Action — a screen question ("what is this button") triggers no desktop action | | | | | | pending |
| Action — unchecking "Let Winly open apps and change volume" stops actions and posts the settings message | | | | | | pending |

## Feature 002 — Desktop Actions (sequences, confirmation, record)

Rows for `specs/002-desktop-actions/quickstart.md` scenarios 1–17. Every one of these is
platform-layer or end-to-end and therefore cannot be covered in CI.

| Scenario | Windows version | Monitor configuration | Scaling factor(s) | Verified by | Date | Result / notes |
|---|---|---|---|---|---|---|
| 1 — M1/US1: with Spotify closed, "open Spotify and play some jazz" opens Spotify **and** plays, from one activation (FR-001) | | | | | | pending — blocked until SPOTIFY_CLIENT_ID/SECRET are set and promoted (T001b) |
| 2 — M1/US1: "open Chrome and put it on the left half" opens, then snaps, in that order (FR-002) | | | | | | pending |
| 3 — M1/US1: "turn it down a bit" still behaves as a single action with no added latency (FR-006, SC-002) | | | | | | pending |
| 4 — M1/US1: "what does this button do?" answers and runs no action | | | | | | pending |
| 5 — M1/US1: a request implying six or more actions stops at five and says the task is unfinished (FR-003) | | | | | | pending |
| 6 — M1/US2: opening an app that is not installed then acting on it stops at the failure; the second action never runs; plain-language report (FR-007, FR-008) | | | | | | pending |
| 7 — M2/US3: "close Word" with unsaved work is stated aloud and listened for; saying **no** leaves Word open (FR-012) | | | | | | pending |
| 8 — M2/US3: same, then silence — after ~5 s it is refused and the sequence stops (FR-012b). **Confirm Winly is not transcribing its own prompt as the answer** | | | | | | pending |
| 9 — M2/US3: "turn the volume down" runs with no confirmation (FR-015) | | | | | | pending |
| 10 — M2/US3: clicking into another window before a typing action lands abandons the typing rather than sending it elsewhere (FR-016, SC-005) | | | | | | pending |
| 10b — M2/US3: a typing action aimed at an elevated window is refused plainly rather than silently dropped by UIPI (FR-021) | | | | | | pending |
| 11 — M2/US1: an app that hangs on a modal (e.g. a pending Spotify update) fails its readiness wait at 60 s, stops the sequence, and reports — never waits forever (FR-005) | | | | | | pending |
| 12 — M2/US4: unchecking the acting permission, restarting, then asking Winly to open something — nothing opens, Winly says actions are off, any question is still answered (FR-017, FR-018) | | | | | | pending |
| 12b — M2/US4: unchecking the acting permission **while a sequence is running** stops it before the next action (FR-019) | | | | | | pending |
| 13 — M3/US5: after several acting requests, the panel's action record lists every attempt with verb, target, outcome and time (FR-022, SC-007) | | | | | | pending |
| 14 — M3/US5: a declined confirmation appears in the record as **Declined**, not absent (FR-023) | | | | | | pending |
| 15 — M3/US5: a typing action's record entry shows the action but **not** the typed text (FR-026, SC-010) | | | | | | pending |
| 16 — M3/US5: the record survives closing and reopening Winly (FR-025) | | | | | | pending |
| 17 — M3/US5: clearing the record leaves it empty (FR-024) | | | | | | pending |
| Readiness — an Electron-style app (Spotify/Discord/Teams) is matched by the live process list, not by the launched stub process, so launch-then-act does not fire early (research.md R1) | | | | | | pending |
| Cost — after the system-prompt edit, `cacheRead` is still non-zero on a second consecutive request (T050) | | | | | | pending |
| STT — "pin Claude to the left" transcribes as "pin", not "pinch"/"punch", and the window snaps (keyterms_prompt + prompt on the streaming socket, 2026-09-13). AssemblyAI accepts unknown query params silently, so the handshake proves nothing; this row is the only proof they are honoured | | | | | | pending |
| STT — after the same change, a misheard command that is still one sound away from a verb is acted on, and a genuinely unintelligible one gets "I didn't catch that" rather than an answer stitched out of screen content | | | | | | pending |
| STT — a request with a thinking pause in the middle ("pin Claude... to the left") comes back as one transcript, not two sentences, and nothing is duplicated (`min_turn_silence=10000`, 2026-09-13) | | | | | | pending |
| STT — an app with an unusual spoken name (Zen Browser, Claude) is transcribed correctly in a command, and a command spoken over background noise still lands (client-side `keyterms_prompt` from the live window list + `voice_focus=near-field`, 2026-09-13) | | | | | | pending |
| Click — "play the first Procreate video" on a YouTube results page in Edge presses that video and it starts playing; the log names the element it pressed (`ScreenClickControl`, 2026-09-13) | | | | | | pending |
| Click — the same request made while the browser has never been touched by an assistive client still works, i.e. Chromium's lazily-built accessibility tree is caught by the poll rather than missed on the first look | | | | | | pending |
| Click — a control whose label reads destructively ("Delete account", "Buy now") is confirmed aloud first; an ordinary link or button is not | | | | | | pending |
| Click — nothing on screen matching the label is reported plainly ("I couldn't find … on your screen to click") rather than claimed as done | | | | | | pending |
| Open — asking to open an app that is already running **and minimised** brings its window back to the front, not just a flashing taskbar button (`AttachThreadInput` fallback in `WindowControl.Focus`, 2026-09-13) | | | | | | pending |
| Sequence — a single hold listing six or more jobs ("open Spotify, put on some jazz, throw Chrome on the left, turn dark mode on, dim the screen, mute it") carries out every one of them in order (cap raised to 10, 2026-09-13) | | | | | | pending |
| Latency — the log's `activation.latency` line now carries its three stages (transcript / first token / speech). Run the same command five times and confirm the three add up to the total and identify the slowest provider (2026-09-13) | | | | | | pending |
| Latency — skipping the transcriber's `Termination` wait (2026-09-13) does not truncate or corrupt a transcript: a one-word command, a long sentence, and a sentence with a mid-thought pause all come back complete, and the `transcript` stage in the line above is shorter than it was before the change | | | | | | pending |
| Follow — with Winly idle and untouched, the companion tracks the cursor everywhere on screen: no stutter, no visible steps, and it settles rather than snapping (`CompositionTarget.Rendering` + exponential approach, 2026-09-13) | | | | | | pending |
| Follow — the motion feels the same on a 60 Hz and a high-refresh display, and stays smooth while another app is busy; the follow is frame-delta driven, so a dropped frame must not make it lurch | | | | | | pending |
| Follow — the companion is noticeably larger than the mouse pointer but still small enough not to cover what is under it, and it trails far enough behind the pointer to leave it clear (28.8x32 DIP, offset 46,40 — shrunk and pushed back on 2026-09-13) | | | | | | pending |
| Follow — moving the cursor across a monitor boundary hands the companion to the next display **at the cursor**, with no flight across the gap; at non-100% scaling it sits at the pointer on both displays, not offset | | | | | | pending |
| Follow — during a pointing answer the companion leaves the cursor, flies its arc to the designated element, then glides back to the cursor when the answer ends (it must not jump back) | | | | | | pending |
| Follow — the companion never blocks a click, a hover tooltip or a drag: the overlay stays click-through while following | | | | | | pending |
| Follow — CPU with Winly idle and the cursor being moved continuously stays negligible (check Task Manager over a minute of movement) | | | | | | pending |
| Confirm — "close Chrome" is confirmed aloud, and answering **"yes"** actually closes it. This never worked: the 5 s window cancelled the transcription instead of closing the microphone, and the transcriber only finalizes a turn once the audio ends, so every yes was read as a refusal (2026-09-13). Two unit tests cover the loop; this row is the only proof against the real microphone and transcriber | Windows 11 Home 10.0.26200 | single monitor `\\.\DISPLAY1`, 1920x1200 physical | 125 % (120 DPI) | User (at the microphone), Claude (log analysis) | 2026-09-14 | PASS - closed on "yes" three times out of three (15:09:54, 15:11:10, 15:13:15), each logging `Confirmation for "Window" heard as yes`, then `Window close on Notepad`, then `Completed`. **The first time this has ever worked.** Notepad was substituted for Chrome: identical code path (window/close, classified consequential), chosen so a failed run could not cost 9 live Chrome processes. |
| Confirm — answering "no", and saying nothing at all, both still leave the action undone, and the sequence stops there rather than hanging for the full grace period | Windows 11 Home 10.0.26200 | single monitor `\\.\DISPLAY1`, 1920x1200 physical | 125 % (120 DPI) | User (at the microphone), Claude (log analysis) | 2026-09-14 | PASS - "no" declined at 15:12:15 and "No, no." at 15:10:15; silence declined at 15:10:49 and 15:12:59, both logging `No confirmation was heard for "Window"`. Every one stopped at the decline rather than hanging. Notepad substituted for Chrome as above. |
| Confirm — the answer is captured cleanly when Winly's own question is coming out of the speakers: the transcript must be the user's word, not the tail of "should I go ahead?" | Windows 11 Home 10.0.26200 | single monitor `\\.\DISPLAY1`, 1920x1200 physical | 125 % (120 DPI) | User (at the microphone), Claude (log analysis) | 2026-09-14 | PASS - across ten confirmations no transcript contained any part of "I'm about to close Notepad. Should I go ahead?". Transcripts were the user's word alone (`yes`, `no`, `No, no.`) or empty. The ordering that buys this - ConfirmAloud awaiting Announce, which awaits playback - held on every run. |
| Fail loud — an action that fails after the answer has already claimed it is **spoken** aloud, not only written to the panel (2026-09-13). Ask for a click on something that is not on screen and confirm the retraction is heard with the panel closed | | | | | | pending |
| Volume — "turn Spotify down a bit" moves **Spotify's own slider inside its window**, visibly, and not the Windows per-app mixer (`SpotifyUiControl.TrySetVolume`, 2026-09-13). Check the slider in the app moved | | | | | | pending |
| Volume — the same request works while Spotify is **paused**, which the mixer path could never do (no audio session exists to attenuate) | | | | | | pending |
| Volume — it is the volume slider that moves and never the playback progress bar: confirm the track does not seek. The match is by the control's name, so a Spotify release that renames it falls back to the mixer and logs the slider names it saw | | | | | | pending |
| Volume — an absolute request ("set Spotify to forty percent") lands at roughly 40% on the app's own slider, whichever range Spotify reports for it (it has shipped both 0-1 and 0-100) | | | | | | pending |
| Click — the model naming the wrong browser ("Edge" while the page is open in Zen or Chrome) still presses the right thing, because the window the user was looking at is used when the named app has no window (2026-09-13). The log says "No window found for … ; clicking in the window that was in front instead" | | | | | | pending |
| STT fast — a single hold spoken quickly, chaining four or more jobs with only a breath between them ("open Spotify put some jazz on throw Chrome on the left and turn the brightness down") comes back as **one** transcript with every job in it, and no word appears twice. A split shows up as duplicated words at the seam, not as missing ones (`max_turn_silence` + `end_of_turn_confidence_threshold=0.9`, 2026-09-14) | | | | | | pending |
| STT fast — the **last word** of a quickly-spoken command survives when the key is released on the final syllable rather than after it (the recorder now drains before the stream is torn down, 2026-09-14). Compare against holding the key half a second longer: the two transcripts must match | | | | | | pending |
| STT fast — a command spoken quickly **while all displays are being captured** (i.e. an ordinary screen question, which encodes every monitor during the hold) has no dropped syllables; this is what MMCSS thread priority on the capture thread is for. Do it on the multi-monitor setup, which is the heaviest case | | | | | | pending |
| STT fast — the `activation.latency` line's `transcript` stage has not regressed by more than one buffer period (~100 ms) against the medians in `tests/score-benchmark.ps1`. The drain wait is the only thing that could have cost anything; if it costs more than that, `DrainTimeout` is being hit and the warning "Microphone capture did not stop cleanly" will be in the log | | | | | | pending |
| STT fast — slow, deliberate speech and one-word commands ("pause", "next") still work exactly as before: refusing to end a turn early must not change anything about a hold that was already one turn | | | | | | pending |
| Retrigger — hold the key again **while Winly is thinking or speaking**: it must stop mid-word, listen, and answer the new request normally. The old answer is abandoned silently — no "I can't reach the answer service" for a request that was replaced (2026-09-14) | Windows 11 Home 10.0.26200 | single monitor `\\.\DISPLAY1`, 1920x1200 physical | 125 % (120 DPI) | User (at the microphone), Claude (log analysis) | 2026-09-14 | PASS - six interruptions in the 21:39:41 to 21:40:33 window, every one logging `Activation abandoned by a newer activation`. **Not one `Activation failed`, and no `WebSocketException` anywhere in the session** - which is the specific tell that the catch is keying off `cancellationToken.IsCancellationRequested` rather than the exception type. Before the 2026-09-14 fix roughly half of these surfaced as a transport error and told the user "I can't reach the answer service" about a request they had themselves replaced. |
| Retrigger — do it five or six times in a row, interrupting each time. Every one of them must still answer; a run that goes silent afterwards means a device was left held, and the log names which | Windows 11 Home 10.0.26200 | single monitor `\\.\DISPLAY1`, 1920x1200 physical | 125 % (120 DPI) | User (at the microphone), Claude (log analysis) | 2026-09-14 | PASS - five interruptions back to back (21:39:41, 21:39:51, 21:39:56, 21:40:00, 21:40:06), then a normal answer at 21:40:19 (3273 ms) and another at 21:40:25 (1971 ms). **The run did not go silent afterwards**, which is the symptom that matters: it would have meant a device was left held. A sixth interruption at 21:40:20 and a seventh at 21:40:33 also recovered. Latencies after the run are in the normal band, so nothing was left in a degraded state either. |
| Retrigger — interrupt **during a confirmation window** ("close Chrome, are you sure?"): the new request is heard, and the app is *not* closed (an interrupted confirmation is a refusal) | Windows 11 Home 10.0.26200 | single monitor `\\.\DISPLAY1`, 1920x1200 physical | 125 % (120 DPI) | User (at the microphone), Claude (log analysis) | 2026-09-14 | PASS - confirmation question posted 21:40:27.616, interrupted 21:40:33.262, logged as `Activation abandoned by a newer activation` and **no `Window close on Notepad` follows**, so the app was not closed. An interrupted confirmation is a refusal, as specified. Note: the abandonment carries an `OperationCanceledException` from the confirmation's own transcription (SendAudio through ConfirmAloud) and Serilog writes its 13-line stack trace to the log. Correct behaviour at the correct level (INF, with the correct message) - but it is 13 lines of noise per interrupted confirmation, and a candidate for logging the exception only at Debug. |
| Retrigger — the new answer plays to the end and is never cut off part-way by the answer it replaced (`NAudioPlayback` now only tears down its own device, 2026-09-14) | Windows 11 Home 10.0.26200 | single monitor `\\.\DISPLAY1`, 1920x1200 physical | 125 % (120 DPI) | User (at the microphone), Claude (log analysis) | 2026-09-14 | PASS (interpretation stated) - user reports the answers were cut off *when the hotkey was pressed*, which is the in-flight answer being abandoned by the new request and is the specified behaviour (001 FR-015). No report of a **new** answer being truncated by the one it replaced, which is what this row tests. Correct this row if the truncation was actually of the replacement. |
| **REGRESSION found 2026-09-14** - `NAudioPlayback.StopIfCurrent` leaks a `WaveOut` and its source stream on every superseded playback, so resources accumulate per activation | Windows 11 Home 10.0.26200 | single monitor `\\.\DISPLAY1`, 1920x1200 physical | 125 % (120 DPI) | Claude (measured) | 2026-09-14 | **FAIL** - fresh process vs the same process after ~35 activations: idle CPU 4.24 % -> 12.63 % of one core, handles 1562 -> 1940, threads 17 -> 32, working set 187 -> 251 MB. Handles/threads/working set trend *down* between activations (GC and pool trimming) but never return to baseline. Cause is exact and in the code: `StopIfCurrent(expected)` returns early when `_output` is no longer `expected`, which correctly declines to tear down the replacement answer's device but also skips disposing its own `output` and `source`. Introduced by the 2026-09-14 fix for the opposite bug (an abandoned answer killing its replacement); the guard needed to be "do not touch the shared field, but always dispose what you own". Leaks hardest during interruptions, which is when `_output != expected`. SC-005 requires idle CPU under 1 %; even the fresh 4.24 % exceeds that, but that portion is the known cursor-follow cost (~3 % documented) and is a separate question from this leak. **PARTIALLY FIXED same day** - `Play`'s finally now discards its own device unconditionally while still refusing to clear the shared field when it is not current. Re-measured over ~30 activations with heavy interruption: threads +7 (was +15), idle CPU 4.65 -> 7.81 % (was 4.24 -> 12.63), working set +104 MB (was +64). **Handles are unchanged at +392, so a second and larger leak remains and it is not the WaveOut.** Ruled out by measurement: it is not sockets - the process held only 6 TCP connections after the run, 3 of them the pooled HTTP/2 connection to the Worker. Next step is to identify the handle type (Sysinternals `handle.exe -p Winly`) before changing anything else. **AUDIO HALF CLOSED 2026-09-14** by the one-device rework above. Measured fresh: idle CPU 3.09 % of a core, against 4.24 % and 4.65 % for the two per-clip builds - so the old code was burning ~1.5 points before a single activation ran. Threads leaked per session fell 15 -> 7 -> 5. **The handle leak is confirmed independent of audio and is still open: +353 over ~26 activations (was +392, +378), essentially unmoved by three separate audio fixes.** It is not sockets (6 TCP connections held after a run, 3 of them the pooled HTTP/2 connection to the Worker) and it is not the playback device. Idle CPU still climbs with it, 3.09 % -> 9.95 %. Next step is `handle.exe -p Winly` to name the handle type before any further change. |
| **OPEN BUG found 2026-09-14** - after roughly 15-20 interruptions in one session, activations begin genuinely failing: `[ERR] Activation failed` with a `WebSocketException` out of `StreamingSpeechToTextProvider.SendAudio`, and the user hears "I can't reach the answer service" | Windows 11 Home 10.0.26200 | single monitor `\\.\DISPLAY1`, 1920x1200 physical | 125 % (120 DPI) | User (at the microphone), Claude (log analysis) | 2026-09-14 | **FAIL** - five occurrences in the 22:15 session against **zero** in the 21:37 session, which ran fewer interruptions. Inner exception is `SocketException (10053)`, an abort by the local host, on `NetworkStream.WriteAsync` while streaming audio up. **This is not the 2026-09-14 misattribution bug**: the catch filter `when (cancellationToken.IsCancellationRequested)` is correct and was verified working in the same sessions (ten abandonments logged correctly). These five had the token *not* cancelled, so they are real failures of a live request, correctly reported. The socket is `using var socket = new ClientWebSocket()` per call and is not shared across activations, so it is not the "abandoned thing tears down its replacement" shape either. Load-dependent and unexplained; likely downstream of the unresolved handle leak above. **Reproduced 2026-09-14 on the reworked build: six failures, same exception.** Better characterised now: they cluster (22:30:43-56 and 22:31:52-22:32:01, three each within ~10 s) during rapid interruption, beginning ~2.5 minutes and roughly 10-15 activations into a session. The message is **"the remote party closed the WebSocket connection"** - AssemblyAI is hanging up, not the client - with a local `SocketException (10053)` on the next write up. Suspect a concurrency or rate limit hit by opening and abandoning a socket per interruption; worth checking the account limits before writing any client-side retry. Note the user-facing text is also wrong: the transcription service dropped, and the message says "I can't reach the answer service". Likely also the cause of the reported "it starts to say something, cuts off, then answers" - the failure message begins, the user re-asks, and the retry succeeds. |
| **OPEN BUG found 2026-09-14** - the beginning of the first word of an answer is clipped | Windows 11 Home 10.0.26200 | single monitor `\\.\DISPLAY1`, 1920x1200 physical | 125 % (120 DPI) | User (at the microphone), Claude (log analysis) | 2026-09-14 | **FAIL, and suspected to be caused by the `Discard` fix recorded above** - reported for the first time in the session immediately after that change and not before it. Mechanism is plausible and unproven: superseded devices are now actually closed, so a `waveOutClose` can land while the next device is starting, and `WaveOut` begins playing on 150 ms of buffer (`BufferMilliseconds` 50 x 3). Before the fix those devices leaked and therefore never closed, so the contention did not exist. **Do not patch this in isolation.** The standing recommendation is to stop creating a `WaveOut` per clip - one device held for the process, fed by a `BufferedWaveProvider` that sentences append into - which addresses this, the leak, and the sentence-boundary gaps together, and is a smaller total change than three separate guards. **FIXED same day - that rework was done.** One `WaveOut` for the process, so nothing closes a device while the next one starts. Confirmed by ear: no clipped first word across ~26 activations. A second, untested part of the same rework closes the related burst on supersede - `ClearBuffer` empties only our provider while the driver still holds ~150 ms of the outgoing answer and plays it out, which sounds like Winly starting to speak and changing its mind, so `Stop()` now also calls `waveOutReset`. **That last part is built and unverified on hardware.** |
| Confirm fast — answering "yes" is acted on within about a second of saying it, not at the end of the 5 s window (2026-09-14). Time it against the old behaviour: the action used to fire ~5 s after the word | Windows 11 Home 10.0.26200 | single monitor `\\.\DISPLAY1`, 1920x1200 physical | 125 % (120 DPI) | User (at the microphone), Claude (log analysis) | 2026-09-14 | FAIL - better than before, short of the bar. Nine "yes" answers across two sessions, measured question-posted to decision: 5.30, 5.53, 6.57, 6.61, 6.95, 7.47, 7.66, 7.87, 8.50 s (median 6.95). Silence baseline in the same runs is 8.49-9.36 s, i.e. question speech (~2.5 s) plus the full 5 s window. So a yes is decided ~2-3.5 s after the word rather than the ~1 s this row requires, and the slowest sample (8.50 s) is indistinguishable from not short-circuiting at all. The onPartial path is firing but not promptly; suspect partial cadence under `mode=max_accuracy` combined with the turn-end levers all pushed to never (2026-09-14 fast-speech work). **Do not tune blind** - log the arrival time of each partial first, so the delay is attributed before anything is changed. |
| Confirm fast — the confirmation question is spoken **after** the answer finishes, cleanly, with no overlap or chopping between the two voices. They used to share one output device and cut each other into fragments | Windows 11 Home 10.0.26200 | single monitor `\\.\DISPLAY1`, 1920x1200 physical | 125 % (120 DPI) | User (at the microphone), Claude (log analysis) | 2026-09-14 | FAIL, cause not yet isolated. Heavy chopping through all ten Block A confirmations. The mechanism this row was written about **is** fixed: Announce awaits `_answerSpeech`, and the log shows the question posted ~2 s after the answer's playback task completed, so there is no overlap. Two candidates remain. (1) A single long no-action answer on a lightly-used process did **not** chop, which argues against per-sentence device churn in `NAudioPlayback.Play` (a new WaveOut per sentence) being sufficient on its own. (2) The `StopIfCurrent` device leak found the same day (see the row below) degrades audio as a session proceeds, and Block A was ten confirmations back to back on an already-used process. **Re-run three confirmations on a freshly started process before investigating further** - if a fresh process does not chop, the leak is the cause of both findings and this row passes once the leak is fixed. **RESOLVED same day - now PASS.** Neither hypothesis was right on its own. The cause was that `NAudioPlayback.Play` opened a fresh `WaveOut` per clip, and an answer is synthesised one sentence at a time, so the audio device was opened and closed between every sentence of every answer. Reworked to one device for the life of the process fed by a `BufferedWaveProvider`, with ownership moved from a reference check to a generation counter. Re-tested by ear over three multi-sentence answers, twenty interruptions and three confirmations: **no chopping anywhere and every answer played**. |
| Confirm fast — a mumbled or unrecognisable answer still waits the full window and then leaves the action undone; only a clear yes/no short-circuits | Windows 11 Home 10.0.26200 | single monitor `\\.\DISPLAY1`, 1920x1200 physical | 125 % (120 DPI) | User (at the microphone), Claude (log analysis) | 2026-09-14 | PASS with a caveat - a deliberate mumble transcribed as "Switch to record. No, no." at 15:14:03 and left Notepad open. Caveat: that phrase contains "no", so it may have short-circuited as a refusal rather than waiting the full window; it does not isolate the unrecognisable case from the clear-no case. Re-run with a mumble carrying no yes/no token to test that properly. |
| Play elsewhere — "play lofi beats on YouTube" opens a **YouTube** search in the default browser, not Spotify, and Winly says it opened a search rather than claiming the track is playing (2026-09-14). Repeat for Apple Music and SoundCloud | | | | | | pending |
| Play elsewhere — "play X" with no service named still goes to Spotify and presses play, exactly as before | | | | | | pending |
